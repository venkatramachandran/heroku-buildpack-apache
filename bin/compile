#!/usr/bin/env bash
# bin/compile <build-dir> <cache-dir>

set -e

BIN_DIR=$(dirname $0)
BUILD_DIR=$1
CACHE_DIR=$2
LP_DIR=`cd $(dirname $0); cd ..; pwd`

# include .files when moving things around
shopt -s dotglob

cd $BUILD_DIR
APACHE_INSTALLED=`which apache2ctl > /dev/null; echo $?`
if [ "$APACHE_INSTALLED" == "1" ] ; then
  #Apache not installed. Install it.
  echo '===========> Installing Apache.'
  fakeroot apt-get update
  fakeroot apt-get install -qy apache2
  fakeroot cp $LP_DIR/conf/*.conf /etc/apache2
  fakeroot service apache2 stop
else
  #Apache is installed. Stop apache2ctl
  echo '===========> Stopping Apache.'
  fakeroot apache2ctl -k stop
  fakeroot cp $LP_DIR/conf/*.conf /etc/apache2
fi

cat >>boot.sh <<EOF
set +x
HOSTS_ENTRY=\`grep -q stack /etc/hosts; echo $?\`
if [ "\$HOSTS_ENTRY" == "1"]; then
  echo '192.168.56.101 stack' >> /etc/hosts
fi
apache2ctl -DNO_DETACH
EOF

chmod +x boot.sh

# clean the cache
rm -rf $CACHE_DIR/*
